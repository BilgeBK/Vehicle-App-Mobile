import React from "react";
import { Text, StyleSheet, View, ScrollView,TouchableOpacity  } from "react-native";

const ViewDetail = ()=>{
    <View>
        <Text>View Detail PAge</Text>
    </View>
};

const style = StyleSheet.create({});

export default ViewDetail;